import { createTheme } from '@mui/material/styles'

export const darkTheme = createTheme({
	palette: {
		mode: 'dark',
		primary: {
			main: '#e44c2a',
		},
		background: {
			default: '#202424',
		},
	},
})
